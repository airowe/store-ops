{
  "name": "@shipaso/spine",
  "version": "0.0.0",
  "private": true,
  "description": "Shared spine for ShipASO (PRD 01): design tokens (SoT), pure honesty helpers, and the transport-agnostic API client. Consumed by both the web and native surfaces.",
  "type": "module",
  "workspaces": [
    "tokens",
    "honesty",
    "api",
    "docpaths",
    "postedge"
  ],
  "scripts": {
    "build:tokens": "node tokens/build.mjs",
    "verify:tokens": "node tokens/verify.mjs",
    "test:honesty": "cd honesty && node --test",
    "typecheck": "tsc -p tsconfig.json",
    "test": "npm run build:tokens && npm run verify:tokens && npm run test:honesty && npm run test:docpaths:unit && npm run test:docpaths && npm run test:postedge && npm run typecheck",
    "test:docpaths": "node docpaths/lint.mjs",
    "test:docpaths:unit": "cd docpaths && node --test",
    "test:postedge": "cd postedge && node --test"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  }
}
