{
  "cli": {
    "version": ">= 5.0.0",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "distribution": "internal",
      "ios": { "simulator": true }
    },
    "production": {
      "autoIncrement": true
    }
  },
  "submit": {
    "production": {
      "ios": {
        "//": "Fill from App Store Connect before `eas submit`. appleId / ascAppId / appleTeamId are required; never commit an app-specific password — use EXPO secrets / interactive auth.",
        "appleId": "APPLE_ID_EMAIL_PLACEHOLDER",
        "ascAppId": "ASC_APP_ID_PLACEHOLDER",
        "appleTeamId": "APPLE_TEAM_ID_PLACEHOLDER"
      },
      "android": {
        "//": "Provide the Play service-account JSON path via an EAS secret (GOOGLE_SERVICE_ACCOUNT_KEY), not a committed file.",
        "track": "internal",
        "releaseStatus": "draft"
      }
    }
  }
}
