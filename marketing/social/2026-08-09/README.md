# Social asset pack — 2026-08-09

Every image here is honest output of the real product or true history — no
mockups, no fabricated screens or numbers. Pair with the four drafted posts:

| Post | Attach |
|---|---|
| 1 — intro ("prove your rank moved", journey link) | `journey-page.png` (hero) or `journey-ledger.png` (the timeline with real milestones) |
| 2 — the rejection story | `rejection-card.png` |
| 3 — the Claude brain | `journey-ledger.png` (the "agent's brain is now Claude" entry is the top item) |
| 4 — ShipShots capture→render loop | `shipshots-set.mp4` (7s vertical) or `set-montage.png` |

## Provenance
- `shots/` — rendered by the ACTUAL shipped pipeline (`scripts/render-shipshots.py
  --bg "#07090e"`, plan in the commit) over the real 6.9" app captures in
  `marketing/screenshots/ios-6.9/en-US/`. Spotlight / editorial / duo-bottom
  frames from the catalog, contrast-guarded signal-green accents.
- `set-montage.png` — the three shots side by side (1600×900, X card size).
- `journey-page.png` / `journey-ledger.png` — screenshots of the live journey
  page markup served locally (2×, 1200×675).
- `rejection-card.png` — designed card (resvg, brand palette); content is the
  true 0.1.0 rejection history from the public ledger.
- `shipshots-set.mp4` — 1080×2346 H.264 slideshow of the three renders + a
  closing card. A social teaser — NOT the Devpost demo video, which must show
  the app running on a real device.
